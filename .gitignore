node_modules/
.repomix/
screenshots/
.claude/settings.local.json
.claude/tdd-guard/
.serena/
.DS_Store
.idea/

# Personal capture data — kept on disk for local dev, excluded from the repo.
# (Self-hostable version will seed/auto-create these on first run.)
bookmarks.json
library.json

# SQLite data dir (Story 1.1+) — created/seeded on first run, never committed.
data/
*.db
*.db-wal
*.db-shm

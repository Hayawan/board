# Backlog: follow-ups from the Epics 13/16/15 batch (PR #2)

Status: in progress — §A done; §B mostly done (SSRF→PR #3, snapshot-delete + backfill-lock→PR #2); §E source fixes done
Created: 2026-06-23
Updated: 2026-06-23
Source: PR #2 (`dev-capture-archive-compose`) — Stories 13.3, 13.4, 16.1, 16.2, 16.3, 15.1, 15.2, 15.3.
Context: captured at batch close so the deferred/staged/known-debt items aren't lost. Everything here was
**consciously deferred and documented** in the relevant story's Dev Agent Record — none of it blocks PR #2.

---

## A. Repo loose ends (cheap, do soon)

- [x] **Land the 8.4 deferred-UX note on `main`.** Done 2026-06-23 — the live-fill/skeleton/degraded UX
  decisions were unstashed and committed to `docs/bmad/stories/8-4-optimistic-save.md` on `main`.
- [x] **Prune the merged `dev-epic-12-17` branch.** Done — deleted, along with a stray merged `06/23/26` branch.

## B. Cross-cutting follow-ups (real debt; fix once at the seam)

- [x] **[security] Capture-layer SSRF denylist — app-wide, pre-existing.** Done in **PR #3** (`dev-ssrf-denylist`).
  `capture/net-guard.ts#assertCapturableUrl` is called at the two seams every server user-URL fetch routes
  through (`dispatchCapture` + the snapshot `capture()`): scheme allowlist + private/loopback/link-local/ULA
  ranges (`net.BlockList`, IPv4-mapped IPv6, numeric-encoded IPv4) + localhost-family hostnames + DNS-resolve
  recheck. Fail-closed. Residuals (DNS-rebinding TOCTOU, octal/mixed numeric encodings) documented in the PR.
  (memory: `board-oss-capture-ssrf`)
- [x] **[correctness] `deleteItemWithAssets` resolved `snapshots/*` under the wrong dir.** Done in **PR #2**
  (`b640655`). Now routes each asset to its dir by the stored path prefix (snapshots/ → the snapshots sibling);
  shared-file guard re-keyed on the full relative path so guard and unlink agree.
- [ ] **[ux/design] Materialized board fields aren't editable (15.3).** STILL OPEN (a design task, not a quick
  fix). The destination descriptor is minimal (`fields:[]`), a deliberate v1 choice (no descriptor-merge across
  heterogeneous sources), so a materialized item's notes/favorite are editable but its descriptor FIELDS are
  not (empty `patchItemFields` allowlist). Divergence still holds. **Fix (design):** choose/merge a descriptor
  for the materialized board so user fields stay PATCHable.
- [x] **[ops] Backfill CLI cross-process Chrome lock (16.3).** Done in **PR #2** (`10029b5`). The CLI now probes
  the configured host:port (`server-lock.ts#isServerListening`) and refuses to start if a server answers
  (override `BOARD_ALLOW_CONCURRENT_CHROME=1`), instead of only a doc warning.

## C. Manual / browser QA owed (logic is tested; runtime is not automatable here)

- [ ] **PWA install + SW-vs-SSE runtime + OS share-sheet (13.3).** The service worker never runs under
  `inject()`. Verify on a real install: the SW registers cleanly, does NOT buffer the `/events` SSE stream
  (it's written to pass `/events` through), and an OS share saves to the Inbox. (Live curl already confirmed
  the static surface + `/share` + SSE headers.)
- [ ] **SingleFile CDP wiring (16.1).** The default `captureHtml` (dynamic-import `single-file-cli`,
  `backEnd:'cdp'` against the existing `browser.wsEndpoint()`) is inspection/manual-verified only. Confirm the
  real `single-file-cli@2.0.83` programmatic API matches the `{initialize, capture, finish}` shape AND that it
  connects to the existing Chrome rather than spawning a second one — **assert the Chrome process count stays 1
  during a real snapshot** (the load-bearing NFR-1 claim).

## E. Type / lint hygiene

- [x] **Two source-level `tsc --strict` gaps.** Done 2026-06-23 on `main` (`fc67fe2`): `processor-library.ts`
  null-coalesces `article.content`; `buildEnrichmentPrompt` accepts the DB row's `unknown` `fields`.
- [ ] **Zero out `tsc --noEmit` and gate it in CI.** ~20 errors remain on `main`, ALL non-blocking
  (CI runs tsx, which strips types). To make `tsc --strict` clean enough to gate:
  - `db/index.ts` — add `@types/better-sqlite3` (devDep; run the Socket score gate first; verify no cascade).
  - `.js`-without-types imports (`collections-ui.js`, `render-map.js`, etc.) — `allowJs: true` (+ `checkJs:false`)
    in `tsconfig.json` clears the `TS7016`s.
  - Test-file `TS2532`/`TS18048` (`.get()` possibly-undefined) and `TS7006` (implicit-any lambda params) in
    `api/v1.test.ts`, `collections-ui.test.ts`, `descriptor/render-map.test.ts`, `db/search.test.ts`,
    `descriptor/inbox-suggest.test.ts` (and the feature branches' new test files) — mechanical `!`/annotations.
  - Then add `tsc --noEmit` to the CI workflow.
- [ ] **Dead code in `server.ts`** (`tsc --noUnusedLocals`): `listCollections` import, `SEED_DESCRIPTORS`,
  `handleGetItems` are declared-but-unused. Deferred while `server.ts` is under active PR #2 review (avoid a
  mid-review conflict); remove after #2 merges.

## D. Staged DOM / SPA wiring (the Epic 8 staging precedent)

- [ ] **Composer + view SPA mounting (15.2 / 15.3).** The read-model, resolver, and propose/accept/materialize
  primitives are tested at the function level; mounting them in the SPA (a composer panel; rendering a view;
  a "materialize" button) is staged DOM.
- [ ] **Composer HTTP accept route (15.2).** `acceptComposerProposal` is exercised at the function level but not
  wired to a route. When wiring it, **re-run `validateProposal` before calling it** (it trusts its input; an
  edited draft could carry an invalid board).
- [ ] **Granular SSE live-fill + skeletons + degraded card (Story 8.4/8.5 deferred-UX).** The confirmed decisions
  (SSE watch + ~2-min timeout fallback wiring `applySseEvent` per-card instead of a full reload; optimistic
  shell after a fast capture; basic-card-only degraded with no Analyze button) live in the stashed 8.4 note
  (see §A). Part of the eventual flat-JSON→SQLite SPA cutover.

---

### Notes
- Severity: §B items are the substantive debt; §A is housekeeping; §C/§D are scoped-out-by-design verification + UI work.
- Each item links back to a story whose Dev Agent Record has the full reasoning and the test/manual split.
